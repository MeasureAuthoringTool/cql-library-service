mongock:
  enabled: false

spring:
  mongodb:
    database: test
    uri: mongodb://test/test

madie:
  security:
    system-api-key: ${MADIE_SYSTEM_API_KEY:test value}
    api-key-header: custom-header
