mongock:
  enabled: false

spring:
  mongodb:
    database: test
    uri: mongodb://test/test

madie:
  security:
    system-api-key: ${MADIE_SYSTEM_API_KEY:a1b2-c3d4-e5f6}
    api-key-header: x-madie-api-key
